apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-backend.fullname" . }}
  labels:
    {{- include "sample-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-backend.selectorLabels" . | nindent 4 }}
